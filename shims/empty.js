// Empty shim for Node built-in modules that are referenced by
// third-party packages (e.g. ws via @supabase/realtime-js) but
// never actually executed in the React Native runtime.
module.exports = {};
